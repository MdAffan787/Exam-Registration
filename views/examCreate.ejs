<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width,initial-scale=1" />
    <title>Exam Registration</title>
    <link rel="stylesheet" href="/css/examCreate.css"/>
</head>
<body>
    <% if (success && success.length > 0) { %> 
        <div class="alert success"
        ><%= success %>
    </div>
     <% } %>
     <% if (error && error.length > 0) { %> 
        <div class="alert error">
            <%= error %>
        </div> 
        <% } %>
    <header>
        <h1>create a new Exam</h1>
        <nav>
            <a href="/">Home</a>
            <a href="/admin/logout">logoout</a>
        </nav>
    </header>
    <main>
        <div class="create">
            <form action="/exam/create" method="post">
               
                <h1>Basic Details</h1> 
                <label>Exam id</label>
                <input type="text" name="examId">
                <label>semester</label>
                <input type="Number" name="semester">
                <label>branch</label>
                <input type="text" name="branch">
                <lable>fees</lable>
                <input type="Number" name="fee">
                <lable>registration start date</lable>
                <input type="date" name="registerStartDate">
                <lable>registration end date</lable>
                <input type="date" name="registerLastDate">
                <div class="subjects">
                    <div class="subject">
                        <label>Subject Code:</label>
        <input type="text" name="subject[subjectCode]" required>

        <label>Subject Name:</label>
        <input type="text" name="subject[subjectName]" required>

        <label>Exam Date:</label>
        <input type="date" name="subject[examDate]" required>

        <label>Duration (hours):</label>
        <input type="number" name="subject[duration]" min="2" max="4" value="3">
                    </div>
                </div>
                 <button type="button" onclick="addsubject()">+ Add Another Subject</button><br><br>
                <input type="submit" value="create">
                


            </form>
            

        </div>
    </main>

    <footer>
        <small>&copy; <%= new Date().getFullYear() %> Exam Registration</small>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>